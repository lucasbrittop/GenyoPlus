{
  "name": "genyo-timer",
  "private": true,
  "version": "1.0.0",
  "description": "Mostra tempo restante do expediente no Genyo",
  "scripts": {
    "dev": "vite build --watch",
    "build": "node scripts/generate-icons.mjs && vite build",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
